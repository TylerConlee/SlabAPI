package config

import "testing"

// TestLoadConfig should:
//  - attempt to load the configuration for slabAPI
//  - if successful, it should return an object with the configuration struct
//  - if no configuration is found, it should create a generic configuration struct
func TestLoadConfig(t *testing.T) {

}
